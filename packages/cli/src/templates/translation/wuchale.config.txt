import { adapter } from "@wuchale/jsx";
import { defineConfig } from "wuchale";

export default defineConfig({
  locales: [{{LOCALES}}],
  adapters: {
    main: adapter({
      loader: "react",
    }),
  },
});
