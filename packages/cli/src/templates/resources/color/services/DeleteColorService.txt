import { inject } from "@ooneex/container";
import { decorator } from "@ooneex/service";
import type { IService } from "@ooneex/service";
import { ColorRepository } from "../repositories/ColorRepository";

type ServiceDataType = {
  id: string;
};

@decorator.service()
export class DeleteColorService<T extends ServiceDataType = ServiceDataType> implements IService<T> {
  constructor(
    @inject(ColorRepository) private readonly repository: ColorRepository,
  ) {}

  public async execute(data?: T): Promise<void> {
    if (!data) return;

    await this.repository.delete({ id: data.id });
  }
}
