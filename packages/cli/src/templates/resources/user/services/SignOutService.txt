import { ClerkAuth } from "@ooneex/auth";
import { inject } from "@ooneex/container";
import { decorator } from "@ooneex/service";
import type { IService } from "@ooneex/service";

type ServiceDataType = {
  sessionId: string;
};

@decorator.service()
export class SignOutService implements IService {
  constructor(@inject(ClerkAuth) private readonly auth: ClerkAuth) {}

  public async execute(data?: ServiceDataType): Promise<void> {
    if (!data) return;

    await this.auth.signOut(data.sessionId);
  }
}
