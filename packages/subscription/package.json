{
  "name": "@ooneex/subscription",
  "version": "0.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "compile": "tsc -p tsconfig.build.json",
    "test": "bun test tests",
    "test:watch": "bun test tests --watch",
    "build": "bun build ./src/index.ts --outdir ./dist --target browser --format esm --packages external --minify",
    "publish": "bun run build && bun publish --access public"
  },
  "dependencies": {
    "@ooneex/currencies": "workspace:*"
  },
  "devDependencies": {
    "@ooneex/types": "workspace:*",
    "@ooneex/image": "workspace:*",
    "@ooneex/tag": "workspace:*"
  },
  "peerDependencies": {},
  "files": [
    "dist/**/*.js",
    "dist/**/*.d.ts"
  ]
}
