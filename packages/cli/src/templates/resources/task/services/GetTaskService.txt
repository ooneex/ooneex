import { inject } from "@ooneex/container";
import { decorator } from "@ooneex/service";
import type { IService } from "@ooneex/service";
import { TaskEntity } from "../entities/TaskEntity";
import { TaskRepository } from "../repositories/TaskRepository";

type ServiceDataType = {
  id: string;
};

@decorator.service()
export class GetTaskService<T extends ServiceDataType = ServiceDataType> implements IService<T> {
  constructor(
    @inject(TaskRepository) private readonly repository: TaskRepository,
  ) {}

  public async execute(data?: T): Promise<TaskEntity | null> {
    if (!data) return null;

    return await this.repository.findOne(data.id);
  }
}
