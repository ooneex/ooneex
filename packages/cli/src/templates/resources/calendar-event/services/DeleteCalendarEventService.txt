import { inject } from "@ooneex/container";
import { decorator } from "@ooneex/service";
import type { IService } from "@ooneex/service";
import { CalendarEventRepository } from "../repositories/CalendarEventRepository";

type ServiceDataType = {
  id: string;
};

@decorator.service()
export class DeleteCalendarEventService implements IService {
  constructor(
    @inject(CalendarEventRepository) private readonly repository: CalendarEventRepository,
  ) {}

  public async execute(data?: ServiceDataType): Promise<void> {
    if (!data) return;

    await this.repository.delete({ id: data.id });
  }
}
