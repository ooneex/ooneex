import { inject } from "@ooneex/container";
import { decorator } from "@ooneex/service";
import type { IService } from "@ooneex/service";
import { TagEntity } from "../entities/TagEntity";
import { TagRepository } from "../repositories/TagRepository";

type ServiceDataType = {
  id: string;
};

@decorator.service()
export class GetTagService<T extends ServiceDataType = ServiceDataType> implements IService<T> {
  constructor(
    @inject(TagRepository) private readonly repository: TagRepository,
  ) {}

  public async execute(data?: T): Promise<TagEntity | null> {
    if (!data) return null;

    return await this.repository.findOne(data.id);
  }
}
