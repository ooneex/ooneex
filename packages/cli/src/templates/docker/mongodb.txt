services:
  # MongoDB - NoSQL document database
  # Docs: https://www.mongodb.com/docs/
  # Connection: mongodb://ooneex:ooneex@localhost:27017/ooneex
  mongodb:
    image: mongo:8.0
    container_name: mongodb
    restart: "on-failure"
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db:rw
    environment:
      - MONGO_INITDB_ROOT_USERNAME=ooneex
      - MONGO_INITDB_ROOT_PASSWORD=ooneex
      - MONGO_INITDB_DATABASE=ooneex

volumes:
  mongodb_data:
