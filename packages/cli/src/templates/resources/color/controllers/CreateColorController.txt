import type { ContextType } from "@ooneex/controller";
import { inject } from "@ooneex/container";
import { ERole } from "@ooneex/role";
import { Route } from "@ooneex/routing";
import type { LocaleType } from "@ooneex/translation";
import { Assert } from "@ooneex/validation";
import { CreateColorService } from "../services/CreateColorService";

type CreateColorRouteType = {
  payload: { name: string; hex: string; description?: string; lang?: LocaleType };
  response: { id: string; name: string; hex: string; description?: string; lang?: LocaleType };
};

@Route.post("/colors", {
  name: "color.create",
  version: 1,
  description: "Create a new color",
  payload: Assert({
    name: "string",
    hex: "string",
    description: "string?",
    lang: "string?",
  }),
  response: Assert({
    id: "string",
    name: "string",
    hex: "string",
    description: "string?",
    lang: "string?",
  }),
  roles: [ERole.USER],
})
export class CreateColorController {
  constructor(
    @inject(CreateColorService) private readonly service: CreateColorService,
  ) {}

  public async index(context: ContextType<CreateColorRouteType>) {
    const { name, hex, description, lang } = context.payload;

    const color = await this.service.execute({ name, hex, description, lang });

    return context.response.json(color);
  }
}
