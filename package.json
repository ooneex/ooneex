{
  "name": "ooneex",
  "private": true,
  "module": "index.ts",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun --workspaces run build",
    "lint": "bun --workspaces run lint",
    "fmt": "bunx biome format --write",
    "test": "bun --workspaces run test",
    "test:watch": "bun --workspaces run test:watch",
    "publish": "bun --workspaces run publish",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.0",
    "@types/bun": "latest",
    "husky": "^9.1.7"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
