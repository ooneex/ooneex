import { DataSource } from "typeorm";
import { AbstractTypeormDatabase, DatabaseException } from "@ooneex/database";
import { injectable } from "@ooneex/container";

@injectable()
export class {{NAME}}Database extends AbstractTypeormDatabase {
  public getSource(database?: string): DataSource {
    database = database || "var/db";

    this.source = new DataSource({
      synchronize: false,
      entities: [
        // Load your entities here
      ],
      enableWAL: true,
      busyErrorRetry: 2000,
      busyTimeout: 30_000,
      database,
      type: "sqlite",
    });

    return this.source;
  }
}
