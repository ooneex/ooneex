import { inject } from "@ooneex/container";
import { decorator } from "@ooneex/service";
import type { IService } from "@ooneex/service";
import { VideoRepository } from "../repositories/VideoRepository";

type ServiceDataType = {
  id: string;
};

@decorator.service()
export class DeleteVideoService<T extends ServiceDataType = ServiceDataType> implements IService<T> {
  constructor(
    @inject(VideoRepository) private readonly repository: VideoRepository,
  ) {}

  public async execute(data?: T): Promise<void> {
    if (!data) return;

    await this.repository.delete({ id: data.id });
  }
}
