import { inject } from "@ooneex/container";
import type { ITypeormDatabase } from "@ooneex/database";
import { decorator } from "@ooneex/repository";
import type { FilterResultType } from "@ooneex/types";
import type { FindManyOptions, FindOptionsWhere, Repository, SaveOptions, UpdateResult } from "typeorm";
import { ILike } from "typeorm";
import { TaskEntity } from "../entities/TaskEntity";

@decorator.repository()
export class TaskRepository {
  constructor(
    @inject("database")
    private readonly database: ITypeormDatabase,
  ) {}

  public async open(): Promise<Repository<TaskEntity>> {
    return await this.database.open(TaskEntity);
  }

  public async close(): Promise<void> {
    await this.database.close();
  }

  public async find(
    criteria: FindManyOptions<TaskEntity> & { page?: number; limit?: number; q?: string },
  ): Promise<FilterResultType<TaskEntity>> {
    const repository = await this.open();

    const { page = 1, limit = 100, q, ...rest } = criteria;

    let skip: number | undefined;
    const take = limit === 0 ? 100 : limit;

    if (page && page > 0 && limit && limit > 0) {
      skip = (page - 1) * take;
    }

    let findOptions = { ...rest, take, ...(skip !== undefined && { skip }) };
    if (q) {
      findOptions = {
        ...findOptions,
        where: {
          ...rest.where,
          title: ILike(`%${q}%`),
        },
      };
    }

    const result = await repository.find(findOptions);

    let countWhere = rest.where;
    if (q) {
      countWhere = {
        ...rest.where,
        title: ILike(`%${q}%`),
      };
    }

    const total = await this.count(countWhere);
    const totalPages = Math.ceil(total / limit);

    return {
      resources: result,
      total,
      totalPages,
      page,
      limit,
    };
  }

  public async findOne(id: string): Promise<TaskEntity | null> {
    const repository = await this.open();

    return await repository.findOne({
      where: { id },
    });
  }

  public async findOneBy(criteria: FindOptionsWhere<TaskEntity>): Promise<TaskEntity | null> {
    const repository = await this.open();

    return await repository.findOne({
      where: criteria,
    });
  }

  public async create(entity: TaskEntity, options?: SaveOptions): Promise<TaskEntity> {
    const repository = await this.open();

    return await repository.save(entity, options);
  }

  public async createMany(entities: TaskEntity[], options?: SaveOptions): Promise<TaskEntity[]> {
    const repository = await this.open();

    return await repository.save(entities, options);
  }

  public async update(entity: Partial<TaskEntity> & { id: string }): Promise<UpdateResult> {
    const repository = await this.open();

    return await repository.update(entity.id, entity);
  }

  public async updateMany(entities: (Partial<TaskEntity> & { id: string })[]): Promise<UpdateResult[]> {
    const repository = await this.open();

    return await Promise.all(entities.map((entity) => repository.update(entity.id, entity)));
  }

  public async delete(
    criteria: FindOptionsWhere<TaskEntity> | FindOptionsWhere<TaskEntity>[],
  ): Promise<UpdateResult> {
    const repository = await this.open();

    return await repository.softDelete(criteria);
  }

  public async count(criteria?: FindOptionsWhere<TaskEntity> | FindOptionsWhere<TaskEntity>[]): Promise<number> {
    const repository = await this.open();

    return await repository.count(criteria ? { where: criteria } : {});
  }
}
