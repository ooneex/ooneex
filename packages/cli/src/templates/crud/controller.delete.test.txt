import { describe, expect, test } from "bun:test";
import { {{CONTROLLER_NAME}}Controller } from "@/controllers/{{CONTROLLER_NAME}}Controller";

describe("{{CONTROLLER_NAME}}Controller", () => {
  test("should have class name ending with 'Controller'", () => {
    expect({{CONTROLLER_NAME}}Controller.name.endsWith("Controller")).toBe(true);
  });

  test("should have 'index' method", () => {
    expect({{CONTROLLER_NAME}}Controller.prototype.index).toBeDefined();
    expect(typeof {{CONTROLLER_NAME}}Controller.prototype.index).toBe("function");
  });
});
