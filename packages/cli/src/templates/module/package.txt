{
  "name": "{{NAME}}",
  "description": "",
  "version": "0.0.1",
  "type": "module",
  "files": [
    "dist",
    "package.json"
  ],
  "scripts": {
    "test": "bun test tests",
    "build": "bunup",
    "lint": "tsgo --noEmit && bunx biome lint"
  }
}
